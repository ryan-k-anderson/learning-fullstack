# Difference between tuple and list tuple = IMMUTABLE/ Not changable

animals = ['Cat', 'Dog', 'Fish']
# can do .reverse(), .append(), .pop()

foods = ('Apple', 'Orange', 'Pizza',)
#Can only do .count(), and .index()
#List that is set for life
